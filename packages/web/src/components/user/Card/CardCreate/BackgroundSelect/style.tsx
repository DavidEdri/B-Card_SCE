import { makeStyles, Theme } from "@material-ui/core/styles";

export const useBackgroundSelectStyles = makeStyles((theme: Theme) => ({
  root: {},
  previewItem: {
    display: "flex",
    justifyContent: "center",
    alignItems: "center",
  },
}));
